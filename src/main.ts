import { getHelloWorld } from "./getHelloWorld";

console.log(getHelloWorld());

Envoyer à Etienne
[(nombre de modules dans la session) * (score meilleur attempt de session) + (score assessments)] / (nombre de modules dans la session + nombre assessments)
